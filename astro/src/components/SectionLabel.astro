---
interface Props {
  text: string;
}
const { text } = Astro.props;
---

<p
  style={{
    fontFamily: 'Inter',
    fontSize: '11px',
    fontWeight: 600,
    letterSpacing: '0.14em',
    textTransform: 'uppercase',
    color: 'var(--primary)',
    margin: '0 0 20px',
  }}
>
  {text}
</p>
